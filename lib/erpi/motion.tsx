'use client';
import {useEffect,useState} from 'react';
import {Pause,Play} from 'lucide-react';
export function MotionLayer(){const[enabled,Enabled]=useState(true);useEffect(()=>{const media=matchMedia('(prefers-reduced-motion: reduce)');const read=()=>{try{Enabled(!media.matches&&localStorage.getItem('erpi-motion')!=='off')}catch{Enabled(!media.matches)}};read();media.addEventListener('change',read);return()=>media.removeEventListener('change',read)},[]);useEffect(()=>{document.documentElement.dataset.motion=enabled?'on':'off'},[enabled]);return <button className="motiontoggle" onClick={()=>{const next=!enabled;Enabled(next);try{localStorage.setItem('erpi-motion',next?'on':'off')}catch{}}} aria-pressed={enabled} aria-label={enabled?'Réduire les animations':'Activer les animations'}>{enabled?<Pause size={12}/>:<Play size={12}/>}<span>Animations {enabled?'actives':'réduites'}</span></button>}
